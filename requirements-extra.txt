# Optional capabilities.
#
# Nothing here is required. Each library unlocks specific sources or analysis,
# and the app detects at runtime what is present -- an absent library shows up
# in the capability report with an install hint, never as a crash or as a
# source that silently returns nothing.
#
# Install everything:        pip install -r requirements-extra.txt
# Or just what you need:     pip install trafilatura praw
#
# Note on size: spacy and transformers pull in large model downloads and are
# too big for most serverless deployments. Install them only where you can
# afford the disk.

# -- Parsing and extraction (small, safe, recommended) ----------------------
beautifulsoup4==4.12.3      # HTML parsing for the page and article collectors
trafilatura==1.12.2         # clean article-body extraction from news pages
httpx==0.27.2               # HTTP/2 client; some CDNs prefer it to requests

# -- Platform clients -------------------------------------------------------
praw==7.8.1                 # Reddit API; avoids the datacenter-IP blocks
Mastodon.py==1.8.1          # authenticated fediverse search
telethon==1.36.0            # public Telegram channel history
tweepy==4.14.0              # X / Twitter API (needs a paid API tier)
instaloader==4.13.1         # public Instagram profiles (heavily rate-limited)
yt-dlp==2024.12.13          # video metadata from YouTube, TikTok and others

# -- Browser automation (local only; will not run on serverless) ------------
playwright==1.62.0          # renders JS-heavy pages, reuses vault sessions
selenium==4.27.1            # fallback driver
scrapy==2.12.0              # multi-page crawls

# -- Analysis ---------------------------------------------------------------
networkx==3.4.2             # centrality and community detection on link maps
pandas==2.2.3               # tabular trend and account analysis

# -- Media and enrichment ---------------------------------------------------
exifread==3.0.0             # EXIF metadata, including GPS, from images
geopy==2.4.1                # geocoding place names
geoip2==4.8.0               # local MaxMind GeoLite2 lookups (offline, no rate limit)

# -- Heavy NLP (large downloads; install deliberately) ----------------------
# spacy==3.8.3              # entity extraction; then: python -m spacy download en_core_web_sm
# transformers==4.47.1      # classification and sentiment; pulls model weights

# -- Unmaintained -----------------------------------------------------------
# snscrape                  # X support broke when X closed its endpoints.
#                           # Left here deliberately: an empty result from it
#                           # means failure, not an absence of posts.
